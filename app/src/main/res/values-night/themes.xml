<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Dark variant, applied automatically in UI_MODE_NIGHT_YES. Splash uses
      the system's dynamic Material You dark-neutral so it matches the M3
      dark surface that Compose paints once the activity is up.
    -->
    <style name="Theme.Lobber" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowSplashScreenBackground">@android:color/system_neutral1_900</item>
    </style>
</resources>
